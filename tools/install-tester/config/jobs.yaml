- match: "3\\..*"
  distros:
    - ubuntu
    - rhel
    - amazon-linux
  outputs:
    enterprise: "Kong Enterprise {{ version }}"
    oss: "{{ version }}"
- match: "2\\..*"
  distros:
    - ubuntu
    - rhel
    - amazon-linux
  outputs:
    enterprise: "Kong Enterprise {{ version }}"
    oss: "{{ version }}"
